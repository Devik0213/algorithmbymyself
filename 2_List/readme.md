# List

```python
class Grades :
	
	def __init__(self):

		self.list = []

	def insert(self, input) : # 처음에 추가.

	def append(self, input) : # 마지막에 추가.

	def getElement(self, position) : # position 위치의 값.

	def remove(self, position) : # 해당 위치 삭제.

	def clear(self) : # 리스트 모든 요소 제거

	def front(self) : # 리스트 첫 요소

	def end(self) : #리스트 마지막 요소

	def prev(self) : #이전 요소

	def next(self) : #다음 요소

```
	